{
  "name": "sns-auto-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for scheduled X posting (ORIMAMA)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
